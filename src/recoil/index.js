import { atom, selector } from 'recoil';

const labelListState = atom({
  key: 'labelListState',
  default: [],
})

const label

const selectedLabelState = atom({
  key: 'selectedLabelState',

})

const labelSelector = selector({
  key: 'labelSelector',
  get: ({ get }) => {
    const labelList
  }
})